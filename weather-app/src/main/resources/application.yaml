spring:
  application:
    name: weather-app

server:
  port: 8080

weather:
  api:
    key: YOUR_OPENWEATHERMAP_API_KEY
    base-url: https://api.openweathermap.org/data/2.5

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html